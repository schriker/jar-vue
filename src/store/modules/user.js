const state = {
  showUserModal: false
}

const mutations = {
  toggleUserModal (state) {
    state.showUserModal = !state.showUserModal
  }
}

const actions = {

}

export default {
  state,
  mutations,
  actions
}
